using System; using Xamarin.Forms;  namespace NavigationNewTest { 	public class LoginPage: ContentPage 	{ 		public LoginPage() 		{ 			/*// Set our view from the "main" layout resource 			SetContentView(Resource.Layout.Login);  			// Get our button from the layout resource, 			// and attach an event to it 			Button button = FindViewById<Button>(Resource.Id.btnLogin);  			button.Click += delegate 			{ 				EditText username = FindViewById<EditText>(Resource.Id.txtUsername); 				EditText password = FindViewById<EditText>(Resource.Id.txtPassword); 				TextView result = FindViewById<TextView>(Resource.Id.txtResult); 				result.Text = "Your username is " + username.Text + " and password is " + password.Text; 			} ;*/ 		} 	} }
